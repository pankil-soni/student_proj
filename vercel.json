{
    "builds": [{
        "src": "loginapp/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "loginapp/wsgi.py"
        }
    ]
}
